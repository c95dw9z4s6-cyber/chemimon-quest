# Chemion Quest v6.1 全要件チェックリスト

作成日: 2026-07-18

状態記号: `[実装済み]` / `[テスト済み]` / `[対象外]` / `[NEEDS_APPROVAL]`

## 0. 正式な作業基準

- 0.1 `[テスト済み]` main、GitHub Pages、正式タグv6.0が正式コミット`0f9d6e9496d381d8110de586c84eae59412ca1e5`を指す。
- 0.2 `[テスト済み]` 正式v6.0だけをコードベースとし、旧ZIP・過去フォルダー・未公開ブランチ・試作・v5系を使用しない。
- 0.3 `[テスト済み]` 指定V3を取得し、サイズ、時間、SHA-256、全デコードを検証する。
- 0.4 `[テスト済み]` 正式V16 loopをファイル名・SHA-256で特定する。
- 0.5 `[テスト済み]` Firebase破壊的変更、危険なセーブ移行、重大矛盾、必須テスト失敗時は公開を停止する。
- 0.6 `[テスト済み]` 実装・全テスト、公開前タグ、main反映、Pages、Firebase対象限定deploy、本番smokeを完了する。
- 0.7 `[テスト済み]` `production-before-v6.1`が正式v6.0を指し、ロールバック手順と復元対象を確認する。本番smoke合格のためロールバック実行は不要。
- 0.8 `[テスト済み]` 無条件の`firebase deploy`、Firestore削除・初期化を行わない。

## 1. v6.1対象

- 1.1 `[テスト済み]` Stage 10タイムアタックと専用ランキングを実装する。
- 1.2 `[テスト済み]` Stage 10のAu・王水・BGM・アニメーション品質を強化する。
- 1.3 `[テスト済み]` v6.2以降の機能を実装しない。

## 2. Stage 10タイムアタック

- 2.1 `[テスト済み]` 通常Stage 10クリア後だけ解放し、通常Stage 10と完全分離する。
- 2.2 `[テスト済み]` 開始時に通常セーブを直接変更しない専用一時状態を作る。
- 2.3 `[テスト済み]` 一時状態はcoin 0、ユニット初期強化、Stage 10未進行、王水初期状態・Lv1、Wave 1とする。
- 2.4 `[テスト済み]` 通常側coin・強化・王水Lvを持ち込まない。
- 2.5 `[テスト済み]` 終了・中断・離脱後に一時状態を破棄し通常セーブへ完全復帰する。
- 2.6 `[テスト済み]` TA中coin、EXP、ユニット・王水強化、Stage進行、Wave報酬、通常ランキング、戦闘状態を通常セーブへ反映しない。
- 2.7 `[テスト済み]` TA専用ローカル自己ベストだけを永続化できる。
- 2.8 `[テスト済み]` 最初に操作可能となった瞬間を単調増加時計で計測開始する。
- 2.9 `[テスト済み]` Au撃破・Stage 10クリア成立で計測終了する。
- 2.10 `[テスト済み]` 召喚・倍速問題、王水操作、操作待ち時間を含める。
- 2.11 `[テスト済み]` 読み込み失敗・エラーで走行を無効化する。
- 2.12 `[テスト済み]` 内部ミリ秒保存、最低1/100秒表示（例3:42.18）とする。
- 2.13 `[テスト済み]` 再読込、終了、長時間background、離脱、再構築、セーブ差替え、ゲストアシスト、開発・テスト状態を公式無効にする。
- 2.14 `[テスト済み]` 負時間、非現実的異常値、走行ID不一致、二重送信を公式無効にする。
- 2.15 `[テスト済み]` 無効時に「この走行は公式記録の対象外です」と表示する。
- 2.16 `[テスト済み]` TA中はゲストアシストを利用不可とし、通常セーブのguestAssistUsedを変更しない。

## 3. TA専用ランキング

- 3.1 `[テスト済み]` 通常ランキングと分離したStage 10 TAランキングを設置する。
- 3.2 `[テスト済み]` 各プレイヤーの自己ベスト1件だけを表示・保持する。
- 3.3 `[テスト済み]` 速い記録だけ更新し、遅い記録で上書きしない。
- 3.4 `[テスト済み]` 同名を内部IDで分離し、表示はニックネーム、内部IDは非表示とする。
- 3.5 `[テスト済み]` tester・自動テスト記録を表示しない。
- 3.6 `[テスト済み]` 通常ランキングへ影響させない。
- 3.7 `[テスト済み]` 本人へベスト、順位、今回更新有無を表示する。
- 3.8 `[テスト済み]` 内部msが速い順、同一msは先に正式登録された順とする。
- 3.9 `[テスト済み]` ローカル自己ベストとオンライン記録を整合させる。
- 3.10 `[テスト済み]` 送信失敗で通常ゲームを止めず、安全に再送できる。
- 3.11 `[テスト済み]` 走行IDで同一走行の多重登録を防ぐ。
- 3.12 `[テスト済み]` 専用コレクション／明確に分離した構造を用いる。
- 3.13 `[テスト済み]` 本人の記録以外を書き換えられず、現記録より速い場合だけ更新できるルールにする。
- 3.14 `[テスト済み]` 新しいログイン方式を追加しない。

## 4. 正式V3

- 4.1 `[テスト済み]` 指定ファイル`chemion_stage10_au_final_boss_bgm_v3(1).mp3`を正式V3として使用する。
- 4.2 `[テスト済み]` 3,747,570 bytes、187.350204秒、SHA-256 `14600796c5beea7b3f81679a8c4bc2c7d535b1448b6985af7b0a857e3bd5c2ba`、全デコード合格を記録する。
- 4.3 `[テスト済み]` 分かりやすい格納名へ変更しても音声本体を再エンコード・加工せずSHA-256を維持する。

## 5. 5の倍数Stage BGM方針

- 5.1 `[テスト済み]` v6.1でV3へ変更するのはStage 10だけとする。
- 5.2 `[テスト済み]` Stage 1〜9のBGM割当を変更せず、Stage 5のV3化はv6.5へ残す。
- 5.3 `[テスト済み]` 5の倍数Stageは原則V3、専用ボス区間は専用曲、Stage 10はV3＋V16、Stage 15以降未確定を引継ぎに記録する。

## 6. Stage 10 BGM

- 6.1 `[テスト済み]` 難易度によらずWave 1〜9はV3、Au形成完了後はV16 loopとする。
- 6.2 `[テスト済み]` Au登場開始でV3を短くダック／フェードし、形成完了に同期してV16を開始する。
- 6.3 `[テスト済み]` Wave 1〜9で通常汎用・難Stage汎用・V16を流さない。
- 6.4 `[テスト済み]` V3とV16を同時再生せず、V16を多重起動しない。
- 6.5 `[テスト済み]` クイズ、王水調製、小overlayで曲頭へ戻さずBGMを再生成しない。
- 6.6 `[テスト済み]` Stage離脱・TA終了後にV16を残さず移動先Stageの既存BGMへ戻す。
- 6.7 `[テスト済み]` BGM ON/OFF、既存音量、ページ表示状態ルールを維持する。
- 6.8 `[テスト済み]` v6.2予定の音量バー・音声設定全面改修を混入しない。

## 7. Au登場演出

- 7.1 `[テスト済み]` Wave 10→停止→味方弾だけ消去→味方状態保持→自陣へ滑らかに押し戻す。
- 7.2 `[テスト済み]` 低速金色波動→遠方粒子収束→79→Au→金→黄金王・Au→段階形成を順に出す。
- 7.3 `[テスト済み]` 形成直前静止・音響ダック→大波動→V16→約3秒相互攻撃停止・保護→再開とする。
- 7.4 `[テスト済み]` 味方を削除・初期化せず、汎用ボス演出と二重発生させない。
- 7.5 `[テスト済み]` 主波、後続波、金箔粒子、空間変形、押し戻し同期、短く弱い振動を組み合わせる。
- 7.6 `[テスト済み]` UI・問題・HPを隠さない。
- 7.7 `[テスト済み]` 登場、金塊圧撃、金箔展開で同じ見た目を使い回さない。

## 8. Au専用モーション

- 8.1 `[テスト済み]` 待機は金箔表面流動、内部質量微動、微粒子循環、過剰点滅なし。
- 8.2 `[テスト済み]` 移動は重量感、短い慣性、瞬間移動なし、描画・当たり判定一致。
- 8.3 `[テスト済み]` 被弾は赤点滅だけにせず薄片剥離、HP段階縮小、欠損非再生、自己再生なし。
- 8.4 `[テスト済み]` 金塊圧撃は質量集積、重心移動、予兆、円形衝撃波、物理表現、ノックバック方向表示、判定非拡大。
- 8.5 `[テスト済み]` 金箔展開は扇形予兆、複数層、波状前進、中央・端厚み差、粒子崩壊、予兆・判定一致、王水と識別。

## 9. 王水調製演出

- 9.1 `[テスト済み]` 実用的な量、器具、投入順、温度、装置、保存、操作を再現しない抽象演出とする。
- 9.2 `[テスト済み]` 条件成立時にHNO₃専用輪郭、HCl別輪郭、4体の光線、HNO₃ 1/1、HCl 3/3、体積比1:3を表示する。
- 9.3 `[テスト済み]` 開始時に軽いBGMダック、中心移動、二系統粒子混合、複層リング、拡大・収束、1:3・体積比を段階表示する。
- 9.4 `[テスト済み]` 生成時に王水、AQUA REGIA、調製成功、専用SE、専用登場モーションを出す。
- 9.5 `[テスト済み]` 読める時間を確保しつつ戦闘を過度に停止しない。
- 9.6 `[テスト済み]` 初回説明／遊び方に「実際に混ぜたり試したりしないでください」を追加し、手順を書かない。

## 10. 王水専用アニメーション

- 10.1 `[テスト済み]` 色違いでなく、浮遊、二種粒子循環、複層逆回転リングを実装する。
- 10.2 `[テスト済み]` 移動残光、攻撃前6粒子、攻撃後回帰、HP低下時の揺らぎ・発光変化を実装する。
- 10.3 `[テスト済み]` 専用登場・移動・攻撃モーションを実装し、HP・本体を発光で隠さない。

## 11. 王水対Au 6段階反応

- 11.1 `[テスト済み]` Au攻撃時だけ専用6段階演出を使い、非Auへ金溶解・粒子離脱・[AuCl₄]⁻・専用文字を出さない。
- 11.2 `[テスト済み]` プレイ中初回だけ約0.7秒停止・軽いダック・王水 VS Au・金の溶解開始・酸化＋塩化物錯体形成・Au→[AuCl₄]⁻を表示する。
- 11.3 `[テスト済み]` 簡略学習表現と明示し、BGMを同位置から復帰する。
- 11.4 `[テスト済み]` 1〜2発は酸化点、3〜4発はCl⁻軌道、5発は金粒子離脱、6発は完了・[AuCl₄]⁻・欠損進行とする。
- 11.5 `[テスト済み]` 戦闘上の6ヒットと演出を同期する。

## 12. 性能・視認性

- 12.1 `[テスト済み]` Canvas統合、適切なeasing、瞬間移動回避、60fps目標とする。
- 12.2 `[テスト済み]` スマホで30fps未満が長時間続かず、性能に応じ粒子数を減らす。
- 12.3 `[テスト済み]` prefers-reduced-motion・軽量時も重要情報を維持する。
- 12.4 `[テスト済み]` UI、問題、選択肢、HPを隠さず、描画・判定を一致させる。
- 12.5 `[テスト済み]` 離脱時に粒子・timer・RAF・listener・AudioNodeを破棄し、メモリリーク・多重起動を防ぐ。
- 12.6 `[テスト済み]` 長い操作不能時間を追加せず、画面揺れを短く弱くする。

## 13. セーブ移行

- 13.1 `[テスト済み]` v6.0のcoin、unit・level、Energy、Stage、Stage 10、王水、実績、学習・復習、設定、BGM、ranking ID、nickname、guestAssistUsed、既存状態を保持する。
- 13.2 `[テスト済み]` TA解放、ローカルbest、走行ID、公式走行中・無効状態、TAランキング記録を追加する。
- 13.3 `[テスト済み]` 移行を冪等にする。
- 13.4 `[テスト済み]` v6.0でStage 10クリア済みならTA自動解放、未クリアならlockを維持する。

## 14. 必須テスト

- 14.1 `[テスト済み]` TAのlock/unlock、一時状態、通常save非上書き、問題中timer、Au停止、中断・reload無効、assist不可、復帰、報酬隔離をテストする。
- 14.2 `[テスト済み]` best更新・遅い記録拒否、1人1件、同名分離、多重送信防止、失敗継続、tester非表示をテストする。
- 14.3 `[テスト済み]` Au演出1回、味方保持、汎用二重なし、波動終了、描画判定、非再生をテストする。
- 14.4 `[テスト済み]` 王水条件・生成文字3種・初回対Au1回・6hit・非Au非表示・TA内動作をテストする。
- 14.5 `[テスト済み]` V3 hash、Wave1〜9 V3、形成後V16、同時再生なし、quiz・調製でrestartなし、離脱復帰、ON/OFF、PWA両音源、Stage1〜9不変、Stage5不変をテストする。
- 14.6 `[テスト済み]` iPhone Safari/PWA、Android Chrome、PC Chrome相当、320/375/390px、縦横、reduced motion、低性能をテストする。
- 14.7 `[テスト済み]` 平均fps、最低付近fps、長時間停止、memory、粒子・AudioNode残留、再入場多重処理を記録する。
- 14.8 `[テスト済み]` ローカル回帰、Firestore emulator 8/8、Firebase対象限定deploy、専用ランキングの本番公開readを確認する。本番テスト記録は送信しない。

## 15. v6.1対象外

- 15.1 `[対象外]` Stage 5 V3化、Stage 1〜9 BGM再編。
- 15.2 `[対象外]` Energy Lv11以上、敗北画面遷移、v5.5〜v5.9履歴、nickname必須・コピー案内、tutorial表示修正。
- 15.3 `[対象外]` tester本番データ削除、アンモニウム殿堂、全SE、音量bar、modal×、諦め報酬、come-back報酬。
- 15.4 `[対象外]` save code短縮、攻略投稿、みんなの統計、無機100問、通常全問題監査。
- 15.5 `[対象外]` Stage11以降、double boss、敵王水、player Au、v6.2〜v7.0機能。

## 16. 将来引継ぎ

- 16.1 `[テスト済み]` v6.5でStage 5をV3へ変更、V3は5の倍数Stage節目BGMと記録する。
- 16.2 `[テスト済み]` Stage 10はV3＋V16、Stage 15以降BGM未確定と記録する。
- 16.3 `[テスト済み]` v7.0後に連続更新を一度停止、Stage11〜15構想保持、Stage15/20 boss順未確定と記録する。
- 16.4 `[テスト済み]` 敵側王水案、プレイヤー側Au案を将来構想としてのみ記録する。

## 17. 仕様漏れ防止

- 17.1 `[テスト済み]` 実装前に本チェックリストを作成する。
- 17.2 `[テスト済み]` 実装後に全項目を実装済み・テスト済み・対象外・NEEDS_APPROVALのいずれかにする。
- 17.3 `[テスト済み]` Firebase本番接続、対象限定deploy、GitHub Pages、本番smokeを確認する。

## 18. 成果物

- 18.1 `[テスト済み]` 実装報告、全要件表、TA、ranking、Au・王水、V3/V16、BGM、性能、Stage1〜10回帰、save移行、Firebase、本番smoke、rollback、変更fileを作る。
- 18.2 `[テスト済み]` SHA-256一覧、配布ZIP、release ZIP、次chat引継ぎを作る。
- 18.3 `[テスト済み]` main、Pages、本番versionと正式音源を照合し、本成果物コミットの公開後に正式tagを付与する。
- 18.4 `[テスト済み]` emulator上の1player 1best、save無破損、本番ランキングread、Stage 10・TA表示を確認する。
